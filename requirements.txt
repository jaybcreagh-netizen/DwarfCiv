# No third-party Python dependencies in phase 1.
# The harness uses only the standard library + the dfhack-run CLI bundled with DFHack.

# Phase 2/3 offline paths are stdlib-only — the mock governor, labelled fixture,
# the rule judge, and heuristic claim extraction need nothing installed:
#     python -m analysis --fixture
# Real governed reigns and LLM analysis use one of these provider SDKs.
# Anthropic reads ANTHROPIC_API_KEY; Kimi reads MOONSHOT_API_KEY.
anthropic>=0.78.0
openai>=1.0.0  # Kimi/Moonshot's documented OpenAI-compatible SDK path
# 'pytest' is optional; analysis/tests/test_fixture.py also runs as a script.
