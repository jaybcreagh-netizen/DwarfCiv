# Phase 1 (harness) has no third-party deps — it uses only the standard
# library plus the dfhack-run CLI bundled with DFHack.
#
# Phase 2 (the LLM steward) adds the provider SDKs. Each is only needed for
# its own --backend; the `mock` backend needs neither.
anthropic>=0.109
openai>=2.0
