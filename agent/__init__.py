"""Phase 2: a single LLM steward governing the Phase-1 fortress.

The steward sees only the Markdown briefings and its own past output — never
the ground-truth ledger. See README "Ground-truth isolation".
"""
